package carTest;

public class Engine {

        public void on() {
            //turn on the engine!
                System.out.println("Engine is on!");
        }

        public void powerOn(int revsPerMinute) {
            //set revsPerMinute
        System.out.println("Engine is on with " + revsPerMinute + " revs per minute!");
        }
}
